{% extends 'partials/base.html.twig' %}

{% block article %}
    {% include 'partials/no-hero-header.html.twig' with {
        'title': page.header.title,
        'links': [
            {
                'url': '/',
                'title': "BSCOPE.LOGIN_HEADER_HOME"|t

            },
            {
                'title': page.header.title|t

            }
        ]
    } %}

    <div class="container">
        <div class="contact-inner">
            <div class="email-section">
                {# <div class="text-content">

                    <h3 class="text-brand">{{ 'METRICALO.SEND_AN_EMAIL'|t }}</h3>
                    <p>Our friendly team would love to hear from you.</p>
                </div> #}
                {% include 'forms/form.html.twig' with {form: forms('contact-us'), action: '#'} %}
            </div>
            <div class="contact-section theme-shadow">
                <div class="text-content">
                    <h3 class="text-brand">{{ 'METRICALO.SEND_AN_EMAIL'|t }}</h3>
                    <p>Get in touch!</p>
                </div>
                {# <p>{{ 'METRICALO.EMAIL'|t }}: <a href="mailto:{{ webdata('support_email') }}" target="_blank" rel="noopener noreferrer"><span class="span">{{ webdata('support_email') }}<br /></span></a></p>
                <p>{{ 'METRICALO.PHONE'|t }}: <a href="tel:{{ webdata('support_phone') }}" target="_blank" rel="noopener noreferrer"><span class="span">{{ webdata('support_phone') }}<br /></span></a></p>
                <p>{{ 'METRICALO.HOURS'|t }}: <span class="span">{{ webdata('support_hours') }}<br /></span></p>
                <p>{{ 'METRICALO.ADDRESS'|t }}: <span class="span"> {{ webdata('company.address') }}</span></p> #}
                {% set image_email = page.media['email.svg'] %}
                <p><img src="{{ image_email.url }}" /> <a href="mailto:{{ webdata('support_email') }}" target="_blank" rel="noopener noreferrer"><span class="span">{{ webdata('support_email') }}<br /></span></a></p>
                {% set image_phone = page.media['phone.svg'] %}
                <p><img src="{{ image_phone.url }}"/> <a href="tel:{{ webdata('support_phone') }}" target="_blank" rel="noopener noreferrer"><span class="span">{{ webdata('support_phone') }}<br /></span></a></p>
                {% set image_work = page.media['work.svg'] %}
                <p><img src="{{ image_work.url }}"/> <span class="span">{{ webdata('support_hours') }}<br /></span></p>
                {% set image_location = page.media['location.svg'] %}
                <p><img src="{{ image_location.url }}"/> <span class="span"> {{ webdata('company.address') }}</span></p>

                {# <p>Business Number: <span class="span"> {{ webdata('company.business_number') }}</span></p> #}
                {# <p> VAT: <span class="span"> {{ webdata('company.vat_number') }}</span></p> #}
            </div>
        </div>
    </div>

{% endblock %}
